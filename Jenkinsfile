pipeline {
    agent any

    stages {
        stage ('Build Image') {
            steps {
                sh 'ls -la'

                //script {
                    //dockerapp = docker.build("alexeiaj/register", '-f ./Dockerfile .')
                //}
            }
        }
    }
}